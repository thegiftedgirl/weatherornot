import React from 'react';
import Styles from './Styles/Styles';

const Styles= (props) => {
    return(
        <main>
        <h1>Styles</h1>
       
        </main>
    );
};

export default Styles;